import React from 'react';

const ReactFile = () => {
    return (
        <div>
            this is the REACT_FILE page to show the react life cycle
        </div>
    );
};

export default ReactFile;